# Core ML
torch>=2.0.0
transformers>=4.40.0
huggingface_hub>=0.23.0
datasets>=2.18.0
accelerate>=0.27.0

# Semantic cache
# On Apple Silicon, install FAISS via conda-forge instead of pip to avoid
# wrapper/binary mismatches with osx-arm64 builds.
faiss-cpu>=1.7.4; not (platform_system == "Darwin" and platform_machine == "arm64")
sentence-transformers>=2.7.0

# Mutator strategies
nltk>=3.8.1
sentencepiece>=0.1.99

# Classical ML baseline
scikit-learn>=1.4.0

# Data handling
numpy>=1.26.0
pandas>=2.2.0

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0

# UI / deployment
flask>=3.0.0

# Notebook
jupyter>=1.0.0
ipywidgets>=8.1.0

# Utilities
tqdm>=4.66.0
